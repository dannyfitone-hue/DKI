import './globals.css';

export const metadata = { title:'RESTOTECH Command', description:'RESTOTECH emergency response and restoration operations platform' };

export default function RootLayout({ children }) { return <html lang="en"><body>{children}</body></html>; }
