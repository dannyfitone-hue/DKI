import './globals.css';

export const metadata = { title:'DKI Restotech Response', description:'RESTOTECH emergency response and restoration operations platform' };

export default function RootLayout({ children }) { return <html lang="en"><body>{children}</body></html>; }
