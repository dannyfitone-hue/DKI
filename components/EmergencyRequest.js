'use client';
import {useState} from 'react';
export default function EmergencyRequest({open,onClose,prefill={},source='public'}){const[form,setForm]=useState({account_name:prefill.account_name||'',address:prefill.address||'',phone:prefill.phone||'',unit:'',note:''});const[state,setState]=useState('idle');if(!open)return null;async function submit(e){e.preventDefault();setState('sending');const r=await fetch('/api/emergencies',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,source})});setState(r.ok?'sent':'error')}return <div className="modalBackdrop"><div className="modal"><div className="row"><div><div className="eyebrow">FAST DISPATCH</div><h2 style={{marginTop:6}}>Emergency Service</h2></div><button className="smallBtn" onClick={onClose}>Close</button></div>{state==='sent'?<div className="success"><h3>RESTOTECH has been notified.</h3><p>Your emergency request is in the dispatch queue. A team member can contact you using the number provided.</p><button className="primaryBtn" onClick={onClose}>Done</button></div>:<form className="form" onSubmit={submit}>{source!=='client_portal'&&<div className="notice">Only the essentials. Address + phone are enough to alert RESTOTECH.</div>}<div className="field"><label>Property Address *</label><input required value={form.address} onChange={e=>setForm({...form,address:e.target.value})} placeholder="123 Main St, Mission Viejo, CA"/></div><div className="field"><label>Best Phone Number *</label><input required value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} placeholder="(949) 555-0123"/></div><div className="grid2"><div className="field"><label>Unit (optional)</label><input value={form.unit} onChange={e=>setForm({...form,unit:e.target.value})} placeholder="204"/></div><div className="field"><label>Quick Note (optional)</label><input value={form.note} onChange={e=>setForm({...form,note:e.target.value})} placeholder="Pipe burst / flooding"/></div></div><button className="emergencyBtn" disabled={state==='sending'}>{state==='sending'?'SENDING…':'🚨 SEND EMERGENCY REQUEST'}</button>{state==='error'&&<p style={{color:'#ff8b92'}}>Could not send. Please try again.</p>}</form>}</div></div>}
